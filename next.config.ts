/** @type {import('next').NextConfig} */
const nextConfig = {
  // لا حاجة لوضع appDir هنا، Next.js 15 يفعل App Router تلقائياً.
  // يمكنك إضافة إعدادات أخرى إن احتجت.
};

export default nextConfig;
